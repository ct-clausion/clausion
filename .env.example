# ============================================
# Clausion 로컬 개발 환경변수 템플릿
# ============================================
# 이 파일을 복사해서 .env 로 저장하세요:
#   cp .env.example .env
# 그 후 각 값을 채워주세요.
# .env 파일은 git에 올라가지 않습니다.
# ============================================

# ── PostgreSQL ──
POSTGRES_DB=classpulse
POSTGRES_USER=classpulse
POSTGRES_PASSWORD=classpulse123

# ── Redis ──
REDIS_HOST=localhost
REDIS_PORT=6379

# ── RabbitMQ ──
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=classpulse
RABBITMQ_PASSWORD=dev_password

# ── OpenAI (팀 공용 키 또는 개인 키) ──
OPENAI_API_KEY=여기에_키_입력

# ── AWS S3 ──
AWS_S3_BUCKET=gb-ljs
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=여기에_키_입력
AWS_SECRET_ACCESS_KEY=여기에_키_입력

# ── LiveKit ──
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=devsecret
LIVEKIT_URL=ws://localhost:7880

# ── JWT ──
JWT_SECRET=classpulse-jwt-secret-key-for-dev-only-change-in-prod

# ── Spring ──
SPRING_PROFILES_ACTIVE=dev

# ── Frontend ──
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws
VITE_LIVEKIT_URL=ws://localhost:7880
