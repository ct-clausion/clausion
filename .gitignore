vibe-coding
.claude-octopus
.claude
.playwright-mcp

# ── Secrets / local config — never commit ──
.env
.env.*
!.env.example
application-dev.yml
application-local.yml
application-*.local.yml
secrets/
credentials.json
*.pem
*.key
!gradle/wrapper/gradle-wrapper.jar.key

### macOS ###
.DS_Store
.AppleDouble
.LSOverride
._*

# Files that might be created on a Trim-enabled NVMe
.Spotlight-V100
.Trashes

### Windows ###
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db

# Folder config file
[Dd]esktop.ini

# Recycle Bin used on FAT drives
$RECYCLE.BIN/

### IDEs (선택 사항이지만 권장) ###
.vscode/
.idea/
*.swp